- name: Install and run mysqld
  hosts: web
  become: yes
  tasks:
    - name: Install mysqld
      ansible.builtin.package:
        name: mysqld
        state: present
    - name: Start mysqld
      ansible.builtin.service:
        name: mysqld
        state: started       