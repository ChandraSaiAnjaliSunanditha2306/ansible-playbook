- name: Install and run mysql
  hosts: web
  become: yes
  tasks:
    - name: Install mysql
      ansible.builtin.package:
        name: mysql
        state: present
    - name: Start mysql
      ansible.builtin.service:
        name: mysql
        state: started        